# Tasks — 2026-W31

요구사항 문서별로 체크리스트를 관리한다. 새 요구사항 문서가 추가되면 그 문서의 작성일 기준으로 섹션을 추가한다. 문서화되지 않은 애드혹 요청(인프라/리팩터링/스타일링 등)은 여기 기록하지 않는다 — 커밋 메시지로 남긴다.

## 2026-07-28 ([3m-log 요구사항_20260728.md](../requirements/2026-W31/3m-log%20요구사항_20260728.md))

### 1. 게스트(비로그인) 이용 지원
- [x] 비로그인 상태로 메인 화면 열람 + 로그 작성 가능
- [x] 게스트 로그는 서버 전송 없이 localStorage에만 저장 (브라우저 네트워크 탭으로 요청 0건 확인)
- [x] 게스트 → 로그인 전환 시 로컬 기록은 이관하지 않음 (이관 로직 자체가 없음 — 코드 상 완전히 분리된 저장소). 유실 가능성은 게스트 배너 문구로 안내

### 2. 마이페이지 (신규 화면)
- [x] 프로필 정보 표시 (이메일 / 닉네임 / 로그인 방식) — `/mypage`
- [x] 내 기록 통계 (총 로그 수, 자주 쓰는 태그) — `GET /devlogs/stats`
- [x] 회원탈퇴 진입 버튼

### 3. 회원탈퇴
- [x] 계정 + 해당 계정의 모든 DevLog 영구 삭제 (복구 불가) — `DELETE /auth/me`, 트랜잭션 처리 + e2e 테스트로 실제 삭제 확인
- [x] (부가) 삭제된 계정의 기존 JWT는 만료 전이어도 이후 요청에서 401 처리되도록 `JwtStrategy`에 계정 존재 확인 추가

### 4. 인기 태그 집계 기준 조정
- [x] 전체 사용자 기준 → 본인 로그 기준으로 되돌리기 (2026-07-23 문서 당시 결정을 재변경). `/devlogs/tags/popular`도 다시 인증 필요로 전환

### 5. 모바일 퍼스트 디자인 개편
- [x] 미니멀 + 터미널 GUI 느낌 톤앤매너로 재정비 (다크 테마 유지) — 데스크톱 브라우저로 실제 확인 완료 (docs/design-system.md)
- [x] 모바일 기준 레이아웃/간격/터치 타겟 크기 재정비 — 코드는 모바일 퍼스트로 작성(44px 터치 타겟 등), 실제 좁은 뷰포트 렌더링은 도구 제약으로 육안 미확인

## 2026-07-28-2 ([3m-log 요구사항_20260728-2.md](../requirements/2026-W31/3m-log%20요구사항_20260728-2.md))

### 1. 로그 작성일 선택 기능
- [x] 로그 작성/수정 시 날짜 선택 가능
- [x] 기본값은 오늘 날짜
- [x] 과거 최대 1개월까지만 선택 가능, 미래 날짜는 선택 불가 — 서버(`assertLogDateInRange`, 유닛+e2e 테스트) + 클라이언트(date input `min`/`max` + 제출 전 검증) 이중 적용. 부가로 다크 테마에서 날짜 피커 아이콘이 안 보이던 것도 `color-scheme: dark`로 같이 고침

### 2. 태그 개수 제한
- [x] 로그 하나당 태그 최대 5개 제한 — 서버(`@ArrayMaxSize(5)` DTO 검증, e2e 테스트) + 클라이언트(입력 최대 개수 도달 시 비활성화, `(N/5)` 카운터 표시) 이중 적용

### 3. 메인 화면 태그 노출 방식 변경
- [x] 태그 칩을 참조 빈도 높은 순으로 정렬 — 서버(`groupBy` + `orderBy count desc`)와 게스트 모드 모두 이미 빈도순 정렬이라 별도 변경 불필요
- [x] 최대 2줄까지만 노출, 나머지는 숨김 처리 — `TagChips`에 `max-height` 기반 2줄 제한 + 실제로 넘칠 때만 나타나는 `[ 더보기/접기 ]` 토글 추가

### 4. 서비스명 확정
- [x] 서비스명 "3m-devlog"로 통일 (화면 제목, 문서 등 혼용 표기 정리)

### 5. 로고 제작
- [x] 터미널 GUI 컨셉 로고 제작 및 적용 (favicon, 화면 헤더) — 터미널 프롬프트(`❯_`) 모티프의 각진 SVG 아이콘, `logo.tsx`(헤더)와 `app/icon.svg`(favicon)가 동일 도형 공유

## 2026-07-30-2 ([3m-log 요구사항_20260730.md](../requirements/2026-W31/3m-log%20요구사항_20260730.md))

서버(스트릭 엔드포인트 + 커서 페이지네이션)와 클라이언트(기간필터 UI + 스트릭 배지 + 무한스크롤)를 병렬 서브에이전트로 나눠 진행 — API 계약(`GET /devlogs/streak` → `{days}`, `GET /devlogs` → `{items, nextCursor}`, 기본 페이지 20)을 먼저 고정한 뒤 동시 작업. 스트릭 단계 전환 기준(1/3/7/14일)은 요구사항 문서에 구체 일수가 없어 사용자에게 직접 확인 후 진행 (`docs/policies.md`에 반영).

### 1. 연속 기록 배지 (스트릭)
- [x] `logDate` 기준 연속 기록 일수 계산 로직 (서버, 로그인 사용자 전용) — `DevLogService.streak()`, `GET /devlogs/streak` → `{ days }`
- [x] 연속 일수 → 아이콘 단계 매핑 (1일 이상 🌱, 3일 이상 🌿, 7일 이상 🪴, 14일 이상 🌳) — `features/devlog/rules/streak.ts`의 `resolveStreakIcon`
- [x] 오늘 미기록이어도 어제까지 연속이면 단계 유지, 끊기면 씨앗 단계로 리셋되는 경계 조건 테스트 — 서버 `devlog.service.spec.ts`의 `describe('streak')` 5케이스 + 클라이언트 `rules/__tests__/streak.test.ts` 경계값 전체
- [x] 메인 화면 헤더 근처에 아이콘 노출 (게스트 모드에는 미노출) — `page.tsx` 타이틀 옆, `useGuestDevLogController`는 항상 `streakIcon: null`. 실제 로그인 세션에서 `/devlogs/streak` 200 응답 및 배지 노출/미노출 조건 브라우저로 확인

### 2. 목록 무한 스크롤
- [x] 로그인 사용자 목록 조회 API에 페이지네이션(커서) 추가 — `GET /devlogs`가 `{ items, nextCursor }` 반환, `cursor`/`limit`(기본 20) 쿼리, `orderBy: [{logDate:'desc'},{id:'desc'}]`로 안정 정렬
- [x] 프론트: 스크롤 하단 도달 시 다음 페이지 로드 — `useDevLogsQuery`를 `useInfiniteQuery`로 전환, `DevLogList`에 `IntersectionObserver` 기반 sentinel + `$ loading more...` 표시
- [x] 게스트 모드는 기존과 동일하게 전체 목록 한 번에 노출 (변경 없음 확인) — `guestStorage.list`/`useGuestDevLogController` 미변경, `hasNextPage: false` 고정

### 3. 날짜 기간 필터
- [x] 메인 화면에 "최근 7일 / 최근 30일 / 전체" 선택 UI 추가, 기본값 "전체" — `page.tsx`에 브래킷 버튼 3개(`Button` variant로 활성 표시)
- [x] 서버: 기간 조건을 기존 태그 필터/키워드 검색과 교집합으로 적용 (로그인 모드) — `DevLogService.findAll`의 `where.logDate.gte`
- [x] 게스트 모드: 클라이언트 측 localStorage 목록에 동일 기간 필터 적용 — `guestStorage.list` + `rules/period.ts`의 `resolvePeriodCutoff` (서버와 동일하게 오늘 포함 N일 규칙)
- [x] 기존 태그 필터·키워드 검색과 함께 적용했을 때 교집합으로 동작하는지 확인 — 서버/클라이언트 양쪽 유닛테스트로 확인

### 검증
- [x] 서버 유닛테스트 32/32, e2e 16/16 통과. 클라이언트 vitest 44/44, `tsc`/`eslint`/`next build` 통과
- [x] 실제 로그인 세션으로 브라우저 확인: 기간 필터 클릭 시 `GET /devlogs?period=...` 정상 호출(브라우저 확장 네트워크 패널이 cross-origin fetch를 안 보여줘서 처음엔 안 됐다고 오판 — `window.fetch` 몽키패치로 재확인해 정상 동작 확인), 스트릭 배지 조건부 노출 정상
- [x] 무한 스크롤 실제 확인 — 실계정에 29일치 테스트 데이터 시딩(`johnkimm994@gmail.com`) 후 브라우저에서 스크롤 시 2페이지(9건)까지 정상 로드됨을 확인. 커서 페이지네이션(`/devlogs`, `/devlogs?cursor=...`)도 직접 호출해 20+9=29건, 두 번째 페이지 `nextCursor: null` 확인. (참고: 자동화 브라우저 탭이 `document.hidden`일 때는 `IntersectionObserver`가 정상적으로 발화하지 않음 — 이는 브라우저의 정상 동작이고 앱 버그 아님. 이 과정에서 sentinel div에 명시적 높이(`h-4`)가 없던 걸 발견해 방어적으로 추가)

## 2026-08-01 ([3m-log 요구사항_20260801.md](../requirements/2026-W31/3m-log%20요구사항_20260801.md))

### 1. AI 회고 요약 & 성장 인사이트
- [x] 헤더 스트릭 배지 근처에 인사이트 진입점 아이콘 추가 (게스트 모드 미노출)
- [x] 모달에서 주간/월간 토글 + 과거 기간 이동(미래 불가) — `InsightModal`, `rules/period-key.ts`
- [x] 요청 시점에 생성, 문장형 요약 + 반복 패턴 + 활동 수치(로그 수) 반환 — `POST /insights/generate`
- [x] 같은 기간 재요청 시 덮어씀(기간당 최신 1건) — `Insight` 유니크 제약 + upsert
- [x] 로그 0건 기간은 AI 호출 없이 안내만 표시 — 서버 422, 프론트 안내 문구
- [x] 마이페이지 "인사이트 기록" 영역 — 주간/월간 최신 결과 카드로 표시, `GET /insights/latest`
- [x] AI 연동은 실제 API 키 없이 로컬 `claude` CLI 서브프로세스로 우회 구현 (`ClaudeCliInsightGenerator`, 포트로 분리해 추후 교체 가능)

### 검증
- [x] 서버 유닛/e2e, 클라이언트 vitest/tsc/eslint/next build 통과
- [x] 실제 로그인 세션으로 브라우저 확인 (생성/빈 기간/미래 기간 차단/마이페이지 기록/재요청 덮어쓰기)
