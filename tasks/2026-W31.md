# Tasks — 2026-W31

요구사항 문서별로 체크리스트를 관리한다. 새 요구사항 문서가 추가되면 그 문서의 작성일 기준으로 섹션을 추가한다. 문서화되지 않은 애드혹 요청은 "요구사항 문서 없음"으로 표시한다.

## 2026-07-28 ([3m-log 요구사항_20260728.md](../requirements/2026-W31/3m-log%20요구사항_20260728.md))

### 1. 게스트(비로그인) 이용 지원
- [x] 비로그인 상태로 메인 화면 열람 + 로그 작성 가능
- [x] 게스트 로그는 서버 전송 없이 localStorage에만 저장 (브라우저 네트워크 탭으로 요청 0건 확인)
- [x] 게스트 → 로그인 전환 시 로컬 기록은 이관하지 않음 (이관 로직 자체가 없음 — 코드 상 완전히 분리된 저장소). 유실 가능성은 게스트 배너 문구로 안내

### 2. 마이페이지 (신규 화면)
- [x] 프로필 정보 표시 (이메일 / 닉네임 / 로그인 방식) — `/mypage`
- [x] 내 기록 통계 (총 로그 수, 자주 쓰는 태그) — `GET /devlogs/stats`
- [x] 회원탈퇴 진입 버튼

### 3. 회원탈퇴
- [x] 계정 + 해당 계정의 모든 DevLog 영구 삭제 (복구 불가) — `DELETE /auth/me`, 트랜잭션 처리 + e2e 테스트로 실제 삭제 확인
- [x] (부가) 삭제된 계정의 기존 JWT는 만료 전이어도 이후 요청에서 401 처리되도록 `JwtStrategy`에 계정 존재 확인 추가

### 4. 인기 태그 집계 기준 조정
- [x] 전체 사용자 기준 → 본인 로그 기준으로 되돌리기 (2026-07-23 문서 당시 결정을 재변경). `/devlogs/tags/popular`도 다시 인증 필요로 전환

### 5. 모바일 퍼스트 디자인 개편
- [x] 미니멀 + 터미널 GUI 느낌 톤앤매너로 재정비 (다크 테마 유지) — 데스크톱 브라우저로 실제 확인 완료 (docs/design-system.md)
- [x] 모바일 기준 레이아웃/간격/터치 타겟 크기 재정비 — 코드는 모바일 퍼스트로 작성(44px 터치 타겟 등), 실제 좁은 뷰포트 렌더링은 도구 제약으로 육안 미확인

## 2026-07-28-2 ([3m-log 요구사항_20260728-2.md](../requirements/2026-W31/3m-log%20요구사항_20260728-2.md))

### 1. 로그 작성일 선택 기능
- [x] 로그 작성/수정 시 날짜 선택 가능
- [x] 기본값은 오늘 날짜
- [x] 과거 최대 1개월까지만 선택 가능, 미래 날짜는 선택 불가 — 서버(`assertLogDateInRange`, 유닛+e2e 테스트) + 클라이언트(date input `min`/`max` + 제출 전 검증) 이중 적용. 부가로 다크 테마에서 날짜 피커 아이콘이 안 보이던 것도 `color-scheme: dark`로 같이 고침

### 2. 태그 개수 제한
- [x] 로그 하나당 태그 최대 5개 제한 — 서버(`@ArrayMaxSize(5)` DTO 검증, e2e 테스트) + 클라이언트(입력 최대 개수 도달 시 비활성화, `(N/5)` 카운터 표시) 이중 적용

### 3. 메인 화면 태그 노출 방식 변경
- [x] 태그 칩을 참조 빈도 높은 순으로 정렬 — 서버(`groupBy` + `orderBy count desc`)와 게스트 모드 모두 이미 빈도순 정렬이라 별도 변경 불필요
- [x] 최대 2줄까지만 노출, 나머지는 숨김 처리 — `TagChips`에 `max-height` 기반 2줄 제한 + 실제로 넘칠 때만 나타나는 `[ 더보기/접기 ]` 토글 추가

### 4. 서비스명 확정
- [x] 서비스명 "3m-devlog"로 통일 (화면 제목, 문서 등 혼용 표기 정리)

### 5. 로고 제작
- [x] 터미널 GUI 컨셉 로고 제작 및 적용 (favicon, 화면 헤더) — 터미널 프롬프트(`❯_`) 모티프의 각진 SVG 아이콘, `logo.tsx`(헤더)와 `app/icon.svg`(favicon)가 동일 도형 공유

## 2026-07-28-3 (요구사항 문서 없음 — 저장소 구조 변경)

### 폴더 구조 평탄화
- [x] `v1/server`, `v1/view` → 저장소 루트 `server/`, `view/`로 이동 (`git mv`로 히스토리 보존). v1, v2 식으로 버전별 폴더를 늘려나가려던 계획을 접고 코드베이스 하나만 관리하는 것으로 변경
- [x] README.md / CLAUDE.md / docs/design-system.md / view/CLAUDE.md의 `v1/server`, `v1/view` 경로 참조를 `server/`, `view/`로 수정
- [x] 이동 후 서버 유닛+e2e 테스트, 프론트 tsc/lint/build 재확인

## 2026-07-28-4 (요구사항 문서 없음 — README 문서화)

### README 배너/배지/스크린샷 추가
- [x] 터미널 타이틀바 + 로고 모티프의 SVG 배너 추가 (`docs/images/banner.svg`)
- [x] 기술 스택 배지(Next.js/NestJS/TypeScript/Prisma/PostgreSQL/Tailwind) 추가
- [x] 게스트 모드 목업 데이터로 캡처한 메인 피드/로그 작성 모달 스크린샷 추가

## 2026-07-28-5 (요구사항 문서 없음 — 워크플로우 문서화)

### tasks/requirements 관리 방식 개선
- [x] `requirements/tasks.md`에 애드혹(문서 없는) 요청 처리 규칙을 CLAUDE.md에 명시
- [x] `requirements/`, `tasks.md`를 ISO 주차 폴더(`YYYY-Www/`) 단위로 재구성
- [x] `tasks/`를 `requirements/`와 별도 최상위 폴더로 분리 (`requirements/YYYY-Www/tasks.md` → `tasks/YYYY-Www.md`) — 이 문서 자체가 그 결과물

## 2026-07-29 (요구사항 문서 없음 — my-client-refactor 스킬 A/B 테스트 및 실적용)

### 스킬 A/B 테스트 (view/, 격리 워크트리 + 병렬 서브에이전트)
- [x] 대상 `view/src/app/page.tsx`, `view/src/components/devlog/devlog-form-modal.tsx`로 좁혀 1라운드 진행 — 결과물 품질은 스킬 유무 거의 동률, 차이는 REVISIT 마커(감사 가능성)뿐
- [x] 1라운드에서 드러난 공백("인증 분기가 컴포넌트를 관통하면 분리해야 하는데 체크리스트엔 없어서 안 건드려짐")을 `my-client-refactor` 스킬(별도 레포 `myeonnnn/my-client-refactor`)에 체크리스트 행으로 반영
- [x] 대화 중 추가로 발견한 네이밍 공백("source"처럼 읽기+쓰기를 함께 캡슐화한 대상에 읽기 전용을 암시하는 이름)도 체크리스트에 반영, PR #5로 스킬 레포에 반영 (사용자 머지 완료)
- [x] 업데이트된 스킬로 2라운드 재실행 — with-skill만 `isAuthed` 8곳 이상 분기를 `DevLogSource` 훅 2개로 완전 분리, without-skill(대조군)은 동일 코드를 보고도 "과한 재구조화"로 판단해 보류 → 체크리스트 한 줄이 실제 결과를 가른 것을 확인
- [x] 실험용 워크트리/브랜치(`ab-test/with-skill`, `ab-test/without-skill`) 정리, Artifact 리포트로 두 라운드 비교 정리

### 실제 코드 적용
- [x] `page.tsx`: `DevLogController` 인터페이스 + `useAuthedDevLogController`/`useGuestDevLogController`로 `isAuthed` 분기를 컨트롤러 선택 한 지점으로 축소, `onSubmit` 인라인 함수를 `handleFormSubmit`으로 추출(파일 내 다른 핸들러와 네이밍 일관성)
- [x] `devlog-form-modal.tsx`: `validateDevLogForm`/`normalizeTag` 순수 함수 추출, `toLogFilename`을 `toDateInputValue` 위임으로 정리(devlog-card.tsx의 동일 구현은 범위 밖이라 REVISIT)
- [x] `npx tsc --noEmit` 통과 확인
- [x] "컨트롤러는 분리했지만 Rules of Hooks 때문에 `enabled` 플래그 주입은 여전히 필요하다"는 트레이드오프를 `page.tsx`에 REVISIT으로 남김 — `AuthedHome`/`GuestHome` 완전 분리(셸 공유 구조 필요)는 후속 결정 사항으로 보류
- [x] 커밋 `4c5564f`

### 모듈 경계 분리 (레이어 구조는 문서화 여부와 무관하게 적용)
- [x] 리뷰 중 지적된 점: 훅/검증 로직/상수를 함수로만 나누고 컴포넌트 파일 안에 그대로 둔 것 — `view/CLAUDE.md`의 `lib/→hooks/→components/→app/page.tsx` 레이어가 있어서가 아니라, 뷰/서비스 로직 분리·프레임워크 비의존 코어 항목이 원래 요구하는 게 모듈 경계 분리라는 결론
- [x] `DevLogController`/`useAuthedDevLogController`/`useGuestDevLogController` → `hooks/use-devlog-controller.ts`(신규)로 이동
- [x] `validateDevLogForm`/`normalizeTag`/`toDateInputValue`/`toLogFilename`/`MAX_TAGS`/`maxLogDate`/`minLogDate` → `lib/devlog-form.ts`(신규)로 이동, `fieldClass`(스타일)는 컴포넌트에 유지
- [x] `npx tsc --noEmit`, `npx eslint` 통과 확인
- [x] `my-client-refactor` 스킬의 "뷰 / 서비스 로직 분리" 항목에 "같은 파일 안 함수 분리로는 부족, 재사용 여부와 무관하게 항상 별도 모듈로" 문구 추가, PR #6으로 스킬 레포에 반영 (리뷰 대기)
